databases:
  - name: snake-and-ladders-db
    databaseName: snake_and_ladders_db
    plan: free

services:
  - type: web
    name: snake-and-ladders-backend
    runtime: python
    plan: free
    buildCommand: pip install -r app/requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: snake-and-ladders-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true

  - type: web
    name: snake-and-ladders-frontend
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    pullRequestPreviewsEnabled: true
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        sync: false
